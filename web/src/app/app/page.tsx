'use client';
import Nav from '../../components/layout/Nav';
import DemoConsole from '../../components/DemoConsole';

/** Renders the public PayLock policy and sandbox audit console. */
export default function AppPage() { return <><Nav /><main id="main" className="min-h-[100dvh] bg-[var(--bg-primary)]"><div className="max-w-7xl mx-auto px-6 md:px-10 lg:px-16 py-24"><div className="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-12"><div><p className="font-mono text-[11px] uppercase tracking-[.2em] text-[var(--text-muted)]">PayLock console</p><h1 className="mt-4 font-heading text-4xl md:text-6xl tracking-tight">Protected actions.</h1></div><DemoConsole compact /></div><div className="grid grid-cols-1 lg:grid-cols-12 gap-8"><section className="lg:col-span-5"><h2 className="font-heading text-2xl mb-4">Grant policy</h2><pre className="font-mono text-xs bg-[var(--bg-surface)] ring-1 ring-[var(--border-default)] rounded-[1rem] p-5 overflow-auto min-h-[20rem]">{'grantee: did:t3n:northwind-treasury\ncontract_id: z:testnet:latch-pay\nfunctions:\n  - pay-invoice\nallowed_hosts:\n  - api.stripe.com\nwindow:\n  valid_from_secs: now\n  valid_until_secs: session'}</pre></section><section className="lg:col-span-7"><h2 className="font-heading text-2xl mb-4">Activity log</h2><div className="bg-[var(--bg-surface)] ring-1 ring-[var(--border-default)] rounded-[1rem] p-6 min-h-[20rem] text-sm text-[var(--text-secondary)]">Run the invoice to write the first row.<div className="mt-6 border-t border-[var(--border-subtle)] pt-4 font-mono text-xs">sandbox · no account data exposed</div></div></section></div></div></main></> }
